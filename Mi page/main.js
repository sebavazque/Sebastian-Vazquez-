let multi =parseInt (prompt("Ingrese un numero para mostrar su table"));
for (let numero = 1; numero <=10; numero++){  
    let resultado = multi * numero;   
    console.log (multi + "x" + numero + "=" + resultado)
}